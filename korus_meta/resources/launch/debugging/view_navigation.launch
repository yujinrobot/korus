<!--
  Used for visualising the korus robot in action. Global navigation, whole environment at view.
  
  Note the robot description should already be published, but if not (i.e. maybe just running
  the base) it reloads it here for convenience.
  
 -->
<launch>
  <node name="rviz" pkg="rviz" type="rviz" args="-d $(find korus_meta)/resources/rviz/view_navigation.rviz"/>
</launch>
